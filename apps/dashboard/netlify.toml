[build]
  command = "cd ../.. && npx turbo run build --filter=@gykmi/dashboard"
  publish = "out"
  ignore = "cd ../.. && git diff --quiet $CACHED_COMMIT_REF HEAD -- apps/dashboard/ packages/ui/ packages/tokens/"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--prefix=/dev/null"
